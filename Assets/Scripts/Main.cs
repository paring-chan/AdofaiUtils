using BepInEx;

[BepInPlugin("io.github.pikokr.adofaiutils2", "AdofaiUtils2", "0.0.1")]
public class Main : BaseUnityPlugin
{
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
